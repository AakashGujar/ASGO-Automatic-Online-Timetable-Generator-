<?php
//storing data into global variable
  float $institute_start_hour			= 7.00;
  float $institute_start_minute		= 0.30;
  $institute_start_amorpm		= "am";
  float $institute_end_hour			= 16.00;
  float $institute_end_minute			= 00;
  $institute_end_amorpm			= "pm";
  float $institute_time_gap			= 0.55;
  $recess_count					= 1;
  float $first_recess_start_hour		= 12.00;
  float $first_recess_start_minute	= 0.30;
  $first_recess_start_amorpm	= "pm";
  float $first_recess_end_hour		= 13.00;
  float $first_recess_end_minute		= 0.00;
  $first_recess_end_amorpm		= "pm";
  float $first_recess_time_gap		= 0.30;
  float $second_recess_start_hour		= 1.00;
  float $second_recess_start_minute	= 0.00;
  $second_recess_start_amorpm	= "pm";
  float $second_recess_end_hour		= 1;
  float $second_recess_end_minute		= 0.00;
  $second_recess_end_amorpm		= "pm";
  float $second_recess_time_gap		= 0.30;
  $daily_lecture_count			= 6;
  $holiday						= 1;
  $closed_day					= 3;
  $halfday						= 1;
  $opened_day					= 5;
  $halfday_lecture_count		= 3;
  $srno=1;

//min+hour
float $institute_start_hour=$institute_start_hour+$institute_start_minute;
 //calculating total number of lecture
$five_days_lecture_count=$daily_lecture_count *5;
$six_days_lecture_count=$daily_lecture_count *6;
$fiveday_plus_halfday_lecture_count=$five_days_lecture_count + $halfday_lecture_count ;
$total_day_count=6;
 //allocating total number of lecture
 if(($holiday == 1)&($halfday == 1)){
	$total_lecture_count=$five_days_lecture_count;
	$total_day_count=5;
 }
 else if(($holiday == 0)&($halfday == 1)){
	$total_lecture_count=$fiveday_plus_halfday_lecture_count;
 }
 else if(($holiday == 0)&($halfday == 0)){
	$total_lecture_count=$six_days_lecture_count;
 }
 float $number_after_point=$institute_start_minute;
//
for($i=$institute_start_hour;$i <= $institute_end_hour;)
{
    if($recess_count == 1)
    {
       echo "<br>".$i;//floor($i);
    }
    else
    {
        echo "<br>";
    }
	float $number_after_point=$number_after_point+$institute_time_gap;
	 if($number_after_point >= "0.60" )
	 {
		float  $minus="0.60";
		float  $number_after_point=$number_after_point - $minus;
		float  $i=floor($i)+1.0;
		float  $i=$i+$number_after_point;
	 }
	 else
	 {
		float  $i=floor($i)+$number_after_point;
	 }	 
	 
}
	/*

$fullpath = 'jklm/file.ext/hhshsh';
$folder = substr($fullpath, 0, strpos($fullpath, '/'));
echo $folder;
// Output => folderName*/
?>