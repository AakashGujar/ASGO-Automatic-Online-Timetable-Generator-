<?php
//session start 
@ob_start();
if (session_status() == PHP_SESSION_NONE) {
    session_start();
}

//connection to database
include('user_connection.php');

/* create product table  */
  $sql_query1="CREATE TABLE IF NOT EXISTS cart
   ( /* Column name                        Column data type */
		cart_product_srno            	   int ,
		cart_product_user_id               text,
		cart_product_id            	  	   int,
		cart_product_image_location        text,
		cart_product_name              	   text,
		cart_product_description      	   text,
		cart_product_quantity         	   int,
		cart_product_price            	   float,
		cart_product_discount_price   	   float,
		cart_product_discount_percentage   float,
		cart_product_rating                int,
		cart_product_catalogue             text
	);";
  $conn->query($sql_query1);/* execution of query */	
?>
